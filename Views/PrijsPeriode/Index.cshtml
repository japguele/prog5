@model IEnumerable<prog5.Models.PrijsPeriode>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.prijs.Naam)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BeginDatum)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EindDatum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.prijs.Naam)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BeginDatum)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EindDatum)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PrijsPeriodeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PrijsPeriodeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PrijsPeriodeId })
        </td>
    </tr>
}

</table>
